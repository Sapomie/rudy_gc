package vfilm

import (
	"context"
	"rudy_gc/internal/types"
)

func (s *DirectoryService) FilmOrganizer(ctx context.Context) (*types.DirDetail, error) {
	//movieTypes, err := s.movieSvc.ListMovieOwned(ctx) //已有相应代码，可以根据时间获取等
	/////...
	//if err != nil {
	//	return nil, err
	//}
	//..前面会根据时间等获取movieTypes:[]*types.MovieType

	return nil, nil
}
