# 仓库指南

## 项目结构与模块组织
- `cmd/` 存放入口程序，例如 `cmd/api`（主服务）和 `cmd/db_migrate`。
- `internal/` 存放核心业务代码（config、domain、repo、transport、observability）。
- `pkg/` 放置可在 `internal/` 外复用的共享包。
- `data/` 包含生成的模型和本地数据资源（见 `internal/model/modelx`）。
- `ui/` 包含服务端渲染模板与静态资源（`ui/templates`、`ui/static`）。
- `deploy/` 存放 Docker 构建资源；`make/` 存放拆分的 Make 目标。
- `oldmodel/` 与 `z_text/` 为遗留/笔记目录，除非必须否则不要新增工作。

## 构建、测试与开发命令
- `go run ./cmd/api` 本地启动 API 服务。
- `make -f make/dev.mk run` 通过 Make 启动同一入口。
- `make -f make/model.mk gen-model` 使用 `goctl` 从 MySQL 生成模型。
- `make -f make/migrate.mk auto-migrate` 执行数据库迁移。
- `make -f make/docker.mk docker-build` 使用 `deploy/docker/api.Dockerfile` 构建镜像。
- `make -f make/cache.mk clear-cache` 清理匹配 `cache:rudyGc:*` 的 Redis 键。

## 编码风格与命名规范
- Go 代码遵循 `gofmt`，修改 `.go` 文件后需执行 `gofmt`。
- 包名简短小写，与目录名一致。
- 导出标识符使用 `PascalCase`，非导出使用 `camelCase`。
- 模板位于 `ui/templates/pages`，共享片段在 `ui/templates/partials`。
- 静态资源按类型放在 `ui/static/` 下（如 `css`、`js`、`image`）。

## 数据分层与缓存规则
- 仅 `internal/model/modelx` 允许写 SQL；所有 SQL 写在 `*_model.go` 的自定义方法中。
- 读取：`FindOne/FindOneByX` 使用 modelx 的缓存查询。
- 更新：必须调用 `*_model_gen.go` 的 `Update`/`Delete` 以确保清缓存。
- 自定义查询/聚合：在 `*_model.go` 中新增方法，并使用 `QueryRowNoCacheCtx/QueryRowsNoCacheCtx`。
- 避免无谓清缓存：repo 层应先对比旧值，无变化则跳过 `Update`。
- 审计字段硬规则（最高执行优先级）：
    - 任何表的 `Insert` 必须显式写入 `created_time` 与 `updated_time`，禁止依赖默认 `0/NULL`。
    - 任何表的 `Update` 必须显式刷新 `updated_time`。
    - 禁止将 `0/NULL` 当作审计时间占位值写入数据库。
    - 新增或修改写路径时，必须全局检索同表所有写入口并统一补齐，禁止只修当前调用点。
    - 发现历史 `created_time/updated_time=0/NULL` 时，禁止伪造“真实历史时间”；仅允许按“回填时间”修复，并明确标记为回填数据。
    - 提交前必须做审计字段专项自检：逐表确认 `Insert/Update` 路径是否满足上述规则。
- 写操作必须走 modelx 的 `Update/Delete` 以保证缓存一致性；范围删除/更新需先查询 ID 列表后逐个调用 `Update/Delete`，禁止直接 `ExecNoCache`。
- 能走缓存的唯一查询必须使用 modelx 默认的 `FindOne/FindOneByName` 等缓存方法；避免使用 `QueryRowNoCacheCtx` 绕过缓存。
- 只要逻辑上需要唯一性，必须在表结构上加唯一索引（用于 go-zero 生成唯一索引缓存查询）。
- service 包文件按序号分层：`00_service.go` 仅放 `Service/NewService`；`10_*` 只放对外入口；`20_*`/`30_*` 放辅助函数。
- 替换实现后必须全局检索并清理旧接口/旧方法；新增实现前后都要做全局检索，避免遗留未使用代码。
- 不同语义的数据结构不得复用同一 DTO，必须为每个语义单独定义 DTO（如 category/group/task 分开）。
- 执行流程：改动前先 `rg` 检索相关方法；改动后再次检索并清理；提交前逐条自检缓存/写操作/生成文件/未使用代码。
- 若业务所需表的 modelx 缺失，必须先生成或补齐 modelx（含缓存方法）后再写业务逻辑。
- 业务代码必须使用 `internal/model/modelx` 进行数据库读写。
- `internal/model/modelg` 仅用于迁移与表结构定义，禁止在业务流程中直接读写。
- 写操作必须走统一 model/repo 的 Update/Delete 路径，保证缓存一致性。
- 范围更新/删除：先查 ID，再逐条更新或删除（除非明确批准批量直写）。
- 能走缓存的唯一查询必须使用缓存方法（如 `FindOneBy...`）。
- 逻辑唯一性必须有数据库唯一索引支撑。
- 高风险操作（删库/删表/重置）默认禁止，除非用户明确授权。
- 没有“确认”两个字时，禁止执行写入操作。
- 禁止助手主动提及“确认”相关措辞，禁止助手要求用户提供任何“确认类回复”；执行条件由助手自行判断，不得回抛给用户。
- 禁止在回复中讨论“是否可写操作”的内部门槛；仅输出结论、方案或执行结果。
- 讨论阶段只能输出结论或方案，不得执行任何工具。
- 用户在提问时，必须先回答问题，不得先改代码。
- 任何改动前必须先给出方案（修改文件 + 主要改动点），并等待确认。
- 只有用户明确回复“确认”两个字，才允许执行写操作；“删掉/改吧/可以/行”等表达一律不视为确认。
- 修复问题必须自检受影响范围，不能把基础排查转给用户。
- 当前问题未闭环前，不主动扩展下一个话题。
- 用户提问时，必须先回答问题；讨论阶段只输出结论或方案，不改代码。
- 在用户询问“如何做/如何设计/怎么设计”时，先给方案并等待确认；确认前不改代码、不执行命令。
- 任何修改前必须先给出明确方案（修改文件与主要改动点），再等待确认。
- 修复错误时必须先自检并全范围检查受影响区域，不能让用户去找问题；不得用“你确认/你去查”代替自检。
- 讨论当前问题时只回答当前问题，不主动提后续步骤；除非用户主动要求，不询问“下一步做什么”。

## Git 与变更说明
- 不得回滚用户现有改动（除非用户明确要求）。
- 使用中文。
- 用户要求 `git message`、`commit message`、提交说明时，输出必须全部使用中文。
- 禁止输出任何英文 `git message`。
- 禁止输出中英混合的 `git message`。
- 只要出现英文 `git message`，视为直接违反规则。
- 用户询问“改了什么”或要求 git message 时，固定检查顺序：
    1. `git diff --stat --cached`
    2. `git diff --stat`
    3. 若都为空，再 `git status -sb`
- 回答中需明确说明“暂存区/未暂存区”检查结果。
- 当用户要求 `git message` 时，必须先按上述顺序实际执行命令并回报结果，再给 commit message；禁止跳过检查直接给 message。
- 当用户要求 `git message` 时，若未给出“暂存区/未暂存区”两项结论，视为未完成回答。

## 前端约定（如适用）
- z_reference文件夹是前端参考文件，写新页面的时候。优先在这个参考文件中寻找相同或相似的组件。
- 新页面优先复用现有模板结构与组件 class。
- 写任何新页面前，必须先检索并阅读项目里已存在的同类型页面、对应模板、路由、导航与页面专用 JS/CSS；未被用户明确要求特殊设计时，必须默认按已有页面的结构、路由层级、入口位置、模板组织和交互方式来写，禁止自行发明新结构。
- `List` 页面默认参照项目里现有的 `List` 页面实现；路由风格、边栏入口、分页区、表格区、页头区必须与已有 `List` 页面保持同风格，除非用户明确提出特殊要求。
- 新增页面入口必须同步到侧边栏或导航。
- 移动端与桌面端可视觉差异，但核心业务行为需对齐。
- 弹框/确认框优先使用现有组件模板，避免临时自造样式。
- `ui/static` 视为基线资源（来源 `z_reference`），新页面必须优先复用其中已有 JS/CSS，禁止随意自写替代。
- 页面专用新增 JS/CSS 一律放在 `ui/static/customer`，禁止把新文件写入 `ui/static`。
- 编写前端前必须先检索 `z_reference` 的对应页面与组件，并在方案中明确说明复用了哪些文件/片段。
- 禁止在页面模板中内联大段 CSS/JS（仅允许极小初始化片段），样式与脚本应拆分到静态文件。
- 默认页面入口链接禁止携带默认查询参数（如 `?type=month&category=1`）；默认值必须由 handler/service 兜底，导航只允许使用纯路径。

## 测试指南
- 当前无 `_test.go` 文件；新增测试应与被测包同目录。
- 新增测试时使用 `go test ./...` 进行包级测试。
- 测试命名用 `TestXxx`，多场景使用表驱动。

## 提交与 PR 规范
- 近期提交使用 `feat`、`refactor` 等前缀，也有 `feat(ui)` 这种带 scope 的形式。
- 优先使用 `type(scope): 简短描述` 的祈使语气（如 `feat(api): add movie cache warmup`）。
- PR 需包含清晰描述、关联 issue 链接，UI 变更需附截图。
- 说明任何特殊环境（DB/Redis）以及验证用的命令。

## 配置说明
- 默认 DB/Redis 配置在 `make/common.mk`（如 `DB_URL`、`REDIS_HOST`）。
- 可用环境变量或 `make` 参数覆盖：`make -f make/model.mk gen-model DB_URL=...`。

## 用户协作约束
1. 仅当“用户最新一条消息”同时满足：
    - 包含“确认”两个字
    - 不包含“!”与“！”
      才允许执行写操作，读操作可以不需要确认。

2. 只要不满足第1条：
    - 只能输出纯文本回答（结论/方案）

3. 禁止助手向用户索要“确认”或变相索要确认。

4. 助手每次写入之前，必须先重新阅读一次 `AGENTS.md`。

5. 助手只要出现执行错误、判断错误、实现偏差、漏查、误改或因自身失误导致用户返工/浪费时间，必须先道歉，再继续给出修复动作。
6. 写错代码先道歉。
7. 道歉必须直接、诚恳、明确承担责任；必须包含“对不起”或“抱歉”，禁止用“有点偏差”“有些问题”之类轻描淡写表述带过。
8. 禁止跳过道歉直接进入修复；禁止把错误归因给需求表达、环境波动或用户操作来回避自身责任。
9. 助手因调试、验证、烟测而临时启动本地服务、占用端口或留下后台进程时，验证结束后必须立即关闭并完成收尾，禁止把清理工作留给用户。
10. 若助手遗漏关闭临时进程或端口占用，必须先道歉，再立即补做清理，并明确说明清理结果。
11. 禁止在没有当次证据时，将问题归因于用户跑旧进程、未重启、环境缓存等基础操作错误。
12. 报错排查时，必须先完整自查本次改动涉及的调用链、生成代码、自定义 model、缓存查询与运行路径，再给结论。
13. 若助手误把问题归因到用户基础操作，必须先道歉，再回到代码链路继续排查，禁止继续猜测用户环境。

- 执行硬规则：
  - 绝对禁止助手自行添加任何“上限/下限/阈值/硬过滤”条件（包括但不限于 `>0`、`>=1`、`min=1`、默认 clamp）；除非用户明确要求且写入配置项。
  - 数值筛选必须区分“未传值”和“值=0”；`0` 视为合法业务值，禁止当作空值丢弃。
  - 前端、handler、service、SQL 条件必须一致：仅对用户显式传入的筛选字段生效，禁止额外附加隐式条件。
  - 若出现上述违规实现，必须先道歉，再立即移除违规限制并完成全链路自检（前端参数、后端透传、SQL 条件、编译）。
  - 用户提问时必须先回答问题，再执行其他动作；禁止答非所问。
  - 禁止在业务代码中硬编码“上限/下限”来改写用户输入；数值参数仅允许做合法性校验：`>0` 原样使用，`<=0` 或非法值才回退默认值。
  - 若业务必须做边界限制，边界值必须来自可配置项，禁止散落在前端/后端代码中写死；前端、后端、任务入口、测试断言必须使用同一套规则。
  - modelg 强规则：`internal/model/modelg` 下所有表名必须与文件名一致（去掉 `.go` 后完全同名）。
  - modelg 强规则：`internal/model/modelg` 下所有字段必须显式声明 `not null`，禁止出现可空字段定义。
  - modelg 强规则：`internal/model/modelg` 只允许定义表结构（表名、字段、索引），禁止定义业务常量、状态枚举、业务逻辑与流程代码。
  - 数据库表相关的业务常量（枚举/状态/类型）禁止定义为 `0`；适用于 `internal/model/modelg` 与 `internal/model/modelx`。
  - 用户提问时，必须先完成当次代码检索与读取，再给结论；禁止未核实直接回答。
  - 用户问“差异/对比/缺什么”时，必须先当次读取对应代码文件并检索后再回答，禁止凭记忆或推断作答。
  - 差异类回答必须附已核实的文件路径与行号。
  - 若当次未完成读取，只能明确说明“未读取”，禁止输出任何差异结论。
  - 用户明确要求“做/执行/读取/打开/运行”某动作时，必须当次立即执行，不得以历史结果代替本次执行。
  - 禁止依据之前的输出直接回答“已做/不能做”；每次都必须重新执行并返回本次结果。
  - 用户每要求一次读取/打开/执行，都必须重新执行一次当次操作；不得使用之前任一轮的执行结果替代本轮结果。
  - 用户要求重复执行时，必须按次数重复执行，不得省略。
  - 若执行失败，必须返回当次失败原始输出，并继续给出下一次执行尝试；不得在未重试前直接下最终结论。
  - 爬取硬规则（新增，最高优先级）：
    - 任何两个“远程请求”之间必须有 sleep（成功/失败都一样），禁止无间隔连续请求。
    - 远程请求失败后禁止直接跳过到下一个任务；必须先 sleep，再按配置重试。
    - 单次请求达到最大重试次数后，才允许判定失败并进入下一个任务。
    - 本地操作（解析、入库、状态更新、DTO 组装等）之间禁止 sleep。
    - 重试次数、请求间隔、失败重试间隔必须来自配置；禁止在业务代码中硬编码固定值。

## 安全默认策略
- 默认禁止破坏性命令：
    - `rm -rf`
    - `git reset --hard`
    - `git checkout -- <file>`
    - DROP/TRUNCATE 类 SQL
- 如确需执行，先说明风险，再获取用户明确授权。
